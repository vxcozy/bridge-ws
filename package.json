{
  "name": "bridge-ws",
  "version": "2.0.2",
  "description": "WebSocket bridge for CLI AI agents — stream Claude, Codex, and Ollama responses over WebSocket",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "bridge-ws": "dist/cli.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node build.js",
    "dev": "tsx watch src/cli.ts",
    "start": "node dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo",
    "prepublishOnly": "npm run clean && npm run build && npm run typecheck && npm test"
  },
  "keywords": [
    "claude",
    "codex",
    "ollama",
    "ai",
    "cli",
    "agent",
    "bridge",
    "claude-code",
    "websocket",
    "streaming"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vxcozy/bridge-ws.git"
  },
  "homepage": "https://github.com/vxcozy/bridge-ws#readme",
  "bugs": {
    "url": "https://github.com/vxcozy/bridge-ws/issues"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "pino": "^10.3.1",
    "pino-pretty": "^13.0.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "@types/ws": "^8.5.0",
    "esbuild": "^0.27.3",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
